<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="top"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <TextView
        android:id="@+id/localIP"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_alignBottom="@+id/listenPort"
        android:layout_alignParentLeft="true"
        android:layout_alignTop="@+id/startListen"
        android:layout_toLeftOf="@+id/listenPort"
        android:gravity="center"
        android:text="0.0.0.0" />

    <EditText
        android:id="@+id/listenPort"
        android:layout_width="70dp"
        android:layout_height="40dp"
        android:layout_alignTop="@+id/startListen"
        android:layout_toLeftOf="@+id/startListen"
        android:ems="10"
        android:inputType="numberDecimal"
        android:text="6666" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/startListen"
        android:layout_width="wrap_content"
        android:layout_height="40dp"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:text="Listen" />

</RelativeLayout>